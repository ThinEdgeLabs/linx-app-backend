# Database Configuration (REQUIRED)
# Used by both Docker Compose and the Rust application
# Application constructs: postgresql://USER:PASSWORD@HOST:PORT/DB
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=bento_alephium
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

RUST_LOG=info

# API Server Configuration (OPTIONAL)
# Host and port for the REST API server
API_HOST=0.0.0.0
API_PORT=8080

# Uncomment to override default node URLs
# DEV_NODE_URL=http://127.0.0.1:12973
# TESTNET_NODE_URL=https://node.testnet.alephium.org
# MAINNET_NODE_URL=https://node.mainnet.alephium.org

# Custom RPC URL (OPTIONAL)
# Uncomment to use a custom blockchain node instead of the default
# RPC_URL=https://your-custom-node.example.com

# Ignored if RPC_URL is set
NETWORK=testnet

# Linx App Configuration (REQUIRED)
GAS_PAYER_ADDRESSES=["gas-payer-address"]
LINX_ADDRESS="linx-address"
LINX_GROUP=0
DIA_ORACLE_ADDRESS="dia-oracle-address"
LINX_API_URL="https://api.linxlabs.org/tokens"
